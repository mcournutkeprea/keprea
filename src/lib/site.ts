/** Domaine canonique du site — source unique pour JSON-LD et métadonnées. Configurable via VITE_SITE_URL (fallback = production). */
export const SITE_URL = import.meta.env.VITE_SITE_URL || "https://www.keprea.com";
